var base = require('../base');
var assert = require('assert');
var Storage = require('../../models/storage.js');

describe('referenced storage', function() {
  it('has id');
  it('has host');
  it('has storeFilePath');
  it('has requestErrorMessage');
  it('fails withut');
  it('fails with incorrect id');
});